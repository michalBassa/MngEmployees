
//   createRoleFormGroup(role: RoleModel): FormGroup {
//     return this._formBuilder.group({
//       roleId: [role.id],
//       startDateRole: [Date, Validators.required],
//       isManagerialRole: [false, Validators.required]
//     });
//   }
//     openRoleFields(roleId: number, event: Event): void {
//     const target = event.target as HTMLInputElement;
//     const isChecked = target.checked;
//      const role = this.rolesList.find(role => role.id === roleId);
//     if (isChecked && role) {
//        this.newRoleFormGroup = this.createRoleFormGroup(role);
//     console.log("createRoleFormGroup")

//     } 
//   }

//   findRoleControl(roleId: number): AbstractControl<any> | null {
//     const control = (this.addEmployeeForm.get('employeeRoles') as FormArray).controls.find(control => control.value.roleId === roleId);
//     return control !== undefined ? control : null;
//   }
// updateIsAdmin(arg0: number,$event: Event) {
//     throw new Error('Method not implemented.');
//     }
 


//  updateRoleFields(roleId: number, event: Event): void {
//   const target = event.target as HTMLInputElement;
//   const isChecked = target.checked;

//   const role = this.rolesList.find(role => role.id === roleId);

//   if (isChecked && role) {
//     const newRoleFormGroup = this.createRoleFormGroup(role);
   
//     (this.addEmployeeForm.get('employeeRoles') as FormArray).push(newRoleFormGroup);
  

//   } else {
//     const roleIndex = (this.addEmployeeForm.get('employeeRoles') as FormArray).controls.findIndex(control => control.value.roleId === roleId);
//     if (roleIndex !== -1) {
//       (this.addEmployeeForm.get('employeeRoles') as FormArray).removeAt(roleIndex);
//     }
//   }
//  }


// updateisManagerialRole(roleId: number, event: Event): void {
//   const target = event.target as HTMLInputElement;
//   const isChecked = target.checked;

//   const roleIndex = this.addEmployeeForm.value.employeeRoles.findIndex((role: any) => role.roleId === roleId);
//   if (roleIndex !== -1) {
//     const isManagerialRoleControl = (this.addEmployeeForm.get(`employeeRoles.${roleIndex}.isManagerialRole`) as FormControl);
//     isManagerialRoleControl.setValue(isChecked);
//   }
// }
// updatestartDateRole(roleId: number, event: Event): void {
//   const target = event.target as HTMLInputElement;
//   const startDate = target.value;

//   const roleIndex = this.addEmployeeForm.value.employeeRoles.findIndex((role: any) => role.roleId === roleId);
//   if (roleIndex !== -1) {
//     const startDateControl = (this.addEmployeeForm.get(`employeeRoles.${roleIndex}.startDateRole`) as FormControl);
//     startDateControl.setValue(startDate);
//   }
// }

// }






// <div formArrayName="employeeRoles">
// <div *ngFor="let role of rolesList"><label>
//   <input type="checkbox" (change)="updateRoleFields(role.id, $event)">
//   {{ role.name }}
// </label>
// <div *ngIf="findRoleControl(role.id)">
//   Inputs for role information
//   <div>
//     <label>Start Date Role:</label>
//     <input type="date" formControlName="startDateRole" (change)="updatestartDateRole(role.id, $event)">
//   </div> 
//   <div>
//     <label>Is Admin:</label>
//     <input type="checkbox" formControlName="isManagerialRole" (change)="updateisManagerialRole(role.id, $event)">
//   </div>
// </div>
// </div>
// </div>

//************************************** */
